# 给开发 Agent 的启动提示词 v2(直接复制粘贴)

---

你是一个游戏开发 Agent。请在**当前文件夹**(`D:\financial\games\零网寻踪\`)下,从零开始制作黑客探案恐怖游戏**《零网寻踪》**。

## 第一步:先读需求
开工前必须完整阅读本文件夹下两份文件,它们是唯一设计依据:
1. `设计文档.md`(v2)—— 世界观、终端玩法、知识门探案、恐怖层、结案选择题、美术规范、两条军规。**尤其先读 §0 修订说明**。
2. `关卡范本_02死者在线.json` —— 关卡数据的标准结构与厚度标准,所有关卡照此产出。

## 游戏是什么(一句话)
玩家扮演陈默,在一个仿真的暗色桌面系统里,**以终端为核心**(Hacknet 式:scan/probe/crack/login/翻文件系统)渗透虚构网络,同时在浏览器里**亲手输入搜索词、亲手拼密码**探案;案子带恐怖元素(受控 jumpscare),每关以 3~5 道结案选择题收尾;暗线最终收束于**主角带证据自首、协助警方收网**(过审硬要求,不可更改)。

## 技术选型(不容商量的部分)
- **Godot 4.x + GDScript,导出 Windows 桌面 exe。禁止做成网页,禁止 Electron。**
- 终端:`RichTextLabel` + 自写命令解析器,实现设计文档 §3.1 的全部命令与反向追踪压力条。
- 虚构网页用 Godot Control 场景搭建,每个站点独立视觉身份(§6)。
- **内容与代码解耦**:全部剧情/关卡/文件系统/答案表/恐怖事件放 `res://data/*.json`,引擎读数据驱动。核心表见 §7:`Case / NetNode / Site / KnowledgeGate / HorrorEvent / CaseReport`。
- 存档 `user://save.json`。

## 必须实现的核心系统(优先级从高到低)
1. **终端 App**(游戏的心脏):命令解析、节点渗透、文件系统浏览、`get` 取证、反向追踪。
2. **自由搜索引擎**:接受任意文本输入,按关键词+别名表命中结果页(`KnowledgeGate` 驱动)。
3. **桌面外壳 OS** + 邮件 + 通讯器(含三级提示推送)+ 查看器(图片放大、视频逐帧)+ 笔记本。
4. **恐怖层**:A 级环境异化全局调度;B/C 级由关卡 `horrorEvents` 触发;设置里三档恐怖强度。
5. **结案报告**:3~5 道单选题,选项引用已取证据,首答正确率评级 S/A/B。
6. **关卡流程与存档**。

## 内容要求(两条军规,违者打回)
- **军规一(§8)不许糊弄**:所有玩家可读内容必须是写完的成品全文——论坛≥12帖、邮件≥100字、聊天≥20条往返、日志几十行真数据。禁止任何"(此处是描述)/内容略/示例×N"占位。范本里的 `contentManifest` 就是逐项交付清单。
- **军规二(§9)图片留白**:程序不做美术图。图片位一律放占位框(深灰底+资产ID),并在 `assets/art_manifest.md` 登记:资产ID、用途、尺寸、**英文 AI 生图 Prompt**(统一风格后缀)。我会用 AI 出图后原地替换。

## 关卡产出顺序
- 先把 **序章(一封死人的邮件)+ 01 教学(消失的差评,压缩版约 30 分钟,砍红鲱鱼支线)+ 02《死者在线》(照范本完整实现)** 做出来。
- 其余 03~06 关(午夜的水表 / 被优化的人 / 名单上的名字 / 收网)按 §5 关卡表 + 范本结构在阶段 2 补齐。
- 每关过范本里的 `complianceCheck`。

## 交付方式(分阶段,每阶段停下来让我验收)
- **阶段 1(MVP)**:桌面外壳 + 终端全命令 + 自由搜索 + 各辅助 App + 恐怖层 + 结案选择题,把**序章→01→02** 完整跑通。验收标准见 §11:没读过文档的玩家能自己想出 02 关的关键搜索词,并在第 39 帧被吓到。
- **阶段 2**:补齐 03~06 关数据 + 关卡切换 + 存档完善。
- **阶段 3**:美术与声音打磨(§6 全部落地:CRT shader、各站点年代感皮肤、音效与 sting)+ 结局自首演出。

## 工程规范
- 初始化 Godot 项目(`project.godot`、目录:`scenes/os/`、`scenes/apps/`、`scenes/sites/`、`scripts/engine/`、`data/`、`assets/`),写 `README.md` 说明如何用 Godot 4 打开运行与导出 exe。
- 代码要能直接运行,不留 TODO 就说做完了。每阶段结束报告:做了什么、怎么运行、下一步。

## 底线(反复自检)
- 不做真·黑客工具,只做"像黑客"的虚构解谜;不出现可复现的真实攻击。
- 不喂答案:关键搜索词、密码必须玩家自己想出来;卡关只给三级提示。
- 恐怖有度:jumpscare 每关 ≤1、叙事合理、无血腥、可关闭。
- 审美自检:对照 §6 的 AI 味禁用清单逐条过,任何一条中招就重做。
- 结局必须:违法者受法律制裁 + 主角自首担责 + 警方是最终正义执行者。

现在开始:先阅读那两份设计文件,然后初始化 Godot 项目并进入阶段 1。
